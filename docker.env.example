# Секреты Docker-версии ASCORA Education (см. BACKUP.md).
#   cp docker.env.example docker.env
# docker.env в git не попадает. Значения — те же, что на Forge (server/.env).
# Адрес базы задавать НЕ нужно: он прописан в compose.yaml.

# openssl rand -base64 48
JWT_SECRET=

# Необязательно: Bitrix24 и Google Sheets — как на Forge.
BITRIX_WEBHOOK=
BITRIX_CATEGORY_ID=
BITRIX_STAGE_ID=
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY_B64=
